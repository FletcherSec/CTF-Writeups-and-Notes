- Desktop Application Streaming Service
- Deliver to any computer, without acquiring, provisioning infrastructure
- The application is delivered from within a web browser

[[WorkSpaces]] provides:
- Full managed VDI and desktop
[[AppStream 2.0]] just provides the application to web browser
- No need to connect to a VDI
- Works for any device with a web browser
- Allows to configure an instance type per application (CPU, GPU, RAM)