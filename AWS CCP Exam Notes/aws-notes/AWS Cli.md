Can be installed by looking up *amazon cli install {operating system}*

Go to IAM > users > {user} > access keys > amazon CLI to make an access key for the cli > after access key is made run `aws configure` in terminal and input key > `aws iam list-users`

[[AWS Cloudshell]] allows for an online terminal hosted in AWS to interface with your AWS console alternative to AWS Cli
- You can upload and download files via `Actions` and `Download file` in cloudshell
- cloudshell only available in some regions
- 